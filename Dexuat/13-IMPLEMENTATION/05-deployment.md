# Deployment

Environments: `DEV → STAGING → PRODUCTION`.

Requirements: Docker, secrets, migrations, backup, health checks, rollback, monitoring, disaster recovery.

Production không dùng mock data mặc định. Version app/API/schema/model cần compatibility matrix.
