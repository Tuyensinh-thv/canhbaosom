# Observability

Metrics: API latency, error rate, queue lag, data freshness, AI latency, inference count, alert delivery, active incidents, resource status.

Logs structured JSON + correlation ID. Trace UI → API → service → model/tool.

Health: Database, API, AI, Rainfall, River, Radar, Camera, Notification.
