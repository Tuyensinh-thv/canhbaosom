# AI Platform

Components: Feature Pipeline; Model Registry; Training Jobs; Inference Services; Evaluation; Monitoring; Explainability.

Lifecycle: `Dataset → Feature → Train → Evaluate → Register → Approve → Deploy → Monitor → Retire`.

Model metadata: id, version, training data version, features, metrics, owner, deployed_at, status.
