# Command Center UI

Screen: header/system health → KPI → WebGIS → active incidents + situation → timeline/resources/scenarios.

KPI: critical zones, active incidents, people at risk, blocked roads, available resources, unacknowledged alerts.

Severity phải dùng text/icon bên cạnh màu để đảm bảo accessibility.
