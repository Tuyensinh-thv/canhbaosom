# Agent Orchestrator

`User/Event → Intent + Context → Select Agent → Tools → Validate Evidence → Compose → Human Approval if required`.

Guardrails: tool allowlist, permission scope, timeout, budget, evidence requirement, audit trace, approval.
